mixin  filer-status(filerStatus)
 each item in filerStatus
            button(
              class=`btn btn-sm ml-1 btn-outline-success ${item.class}`,
              button-status=item.status
            ) #{item.name}